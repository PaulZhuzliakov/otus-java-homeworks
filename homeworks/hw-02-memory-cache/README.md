# hw-02-memory-cache

Кэш текстовых файлов на SoftReference и WeakReference.
Интерфейс `Cache<K,V>`, две реализации, консольный `cache/menu/Emulator`.

```bash
mvn test
mvn -q compile exec:java -Dexec.mainClass=cache.menu.Emulator
```

Для проверки эмулятора есть файлы в `demo/`.
